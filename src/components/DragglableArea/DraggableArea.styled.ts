import styled from 'styled-components';

export const DraggableAreaStyled = styled.section`
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  position: relative;
  cursor: grab;
  background-color: #f0f0f0;
`;
